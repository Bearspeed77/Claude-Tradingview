{
  "name": "tradingview-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP server for a crypto chart-analysis workflow, driven by rules.json",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test test/*.test.js",
    "smoke": "node scripts/smoke.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "engines": { "node": ">=20" }
}
